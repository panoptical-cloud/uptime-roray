export type Server = {
    id: string
    name: string
    hostname: string
    ip: string
    agent_port: number
    agent_version: string
  }
  
  export type ServerGroup = {
    id: number
    name: string
    desc: string
  }